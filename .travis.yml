branches:
  only:
    - Travistest    
jobs:
  include:
    - stage: Test core
      os: linux
      dist: bionic
      language: matlab
      script: matlab -batch 'addpath(genpath(pwd));r=runtests("dev/travis/core");assert(~any([r.Failed]))'      
    - stage: Test core
      os: linux
      dist: bionic
      language: matlab      
      script: matlab -batch 'addpath(genpath(pwd));r=runtests("dev/travis/operators");assert(~any([r.Failed]))'